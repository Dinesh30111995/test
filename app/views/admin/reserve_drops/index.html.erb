<div class="main-content">
  <div class="page-content">
    <div class="container-fluid">

      <div class="row">
        <div class="col-12">
          <div class="card">
            <div class="card-body">
              <div class="row">
                <div class="col-sm-6">
                  <h3 style="color:orangered"><%=flash[:notice]%></h3>
                  <h4 class="card-title"><%= t("admin.reserve_drops.index.title") %></h4>
                </div>
                <div class="col-sm-2"></div>
                <div class="col-sm-4">
                  <%= link_to t("admin.reserve_drops.index.add_new"), new_admin_reserve_drop_path, class: "float-right btn btn-primary" %>
                </div>
              </div>
              <table id="datatable-buttons" class="table table-striped table-bordered dt-responsive nowrap" style="border-collapse: collapse; border-spacing: 0; width: 100%;">
                <thead>
                  <tr>
                    <!-- <th><%#= t("admin.reserve_drops.index.id") %></th> -->
                    <th><%= t("admin.reserve_drops.index.name") %></th>
                    <th><%= t("admin.reserve_drops.index.minimum_bid") %></th>
                    <th><%= t("admin.reserve_drops.index.starts_at") %></th>
                    <th><%= t("admin.reserve_drops.index.expires_in") %></th>
                    <th><%= t("admin.reserve_drops.index.address") %></th>
                    <th><%= t("admin.reserve_drops.index.category") %></th>
                    <th>Action</th>
                  </tr>
                </thead>
                <tbody>
                  <% if @reserve_drops.present? %>
                    <% @reserve_drops.excluding(:edition_drop).each do |fcollection| %>
                      <tr>
                        <!-- <td><%#= fcollection.id %></td> -->
                        <td><%= fcollection.collection.name %></td>
                        <td><%= fcollection.min_bid_price %></td>
                        <td><%= fcollection.starts_at&.strftime("%Y-%m-%d %H:%M:%S") %></td>
                        <td><%= fcollection.expires_in.strftime("%Y-%m-%d %H:%M:%S") %></td>
                        <td><%= fcollection.collection.address %></td>
                        <td><%= fcollection.collection.category.join(',') %></td>
                        <td>
                          <%= link_to "Destroy", admin_reserve_drop_path(fcollection), method: :delete, class: "pr-2" %>
                        </td>
                      </tr>
                    <% end %>
                  <% else %>
                    <tr>
                      <td colspan="4" class="text-center"><%= t(:no_records, scope: :admin) %></td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>

      <div class="row">
        <div class="col-12">
          <div class="flickr_pagination">
            <%= will_paginate @reserve_drops %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
